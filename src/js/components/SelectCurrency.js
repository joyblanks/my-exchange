import React from 'react';

const SelectCurrency = (props) => {
  
    return (
        <select multiple>
            <option>USD</option>
            <option>GBP</option>
            <option>EUR</option>
        </select>
    );
}

export default SelectCurrency;
